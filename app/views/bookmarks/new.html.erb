<h1>Add your own Bookmark</h1>

<%= simple_form_for([@list, @bookmark]) do |f| %>
  <%= f.input :comment, as: :text %>
  <%= f.association :movie %>
  <%= f.submit 'Submit your bookmark' %>
<% end %>
